import React from "react";
import styled from "styled-components";

const FlexComponent = styled.div`
  display: flex;
  justify-content: center;
`;

const ComponentForCenterAlign = () => {
  return <FlexComponent />;
};

export default ComponentForCenterAlign;
