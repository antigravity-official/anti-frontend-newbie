import React from "react";
import styled from "styled-components";

const MainTitle = styled.h1`
  font-size: 2rem;
`;

const Title = () => {
  return <MainTitle>원/유로 환율 계산기</MainTitle>;
};

export default Title;
