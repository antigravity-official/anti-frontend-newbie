import { Icon } from "@iconify/react";

const Loading = () => {
  return (
    <div className="loading-icon__container">
      <Icon className="loading-icon" icon="eos-icons:loading" />
    </div>
  );
};

export default Loading;
